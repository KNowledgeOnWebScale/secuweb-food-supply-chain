## firefly reset

Clear all data in a stack

### Synopsis

Clear all data in a stack

This command clears all data in a stack, but leaves the stack configuration.
This is useful for testing when you want to start with a clean slate
but don't want to actually recreate the resources in the stack itself.
Note: this will also stop the stack if it is running.


```
firefly reset <stack_name> [flags]
```

### Options

```
  -f, --force   Reset the stack without prompting for confirmation
  -h, --help    help for reset
```

### Options inherited from parent commands

```
      --ansi string   control when to print ANSI control characters ("never"|"always"|"auto") (default "auto")
  -v, --verbose       verbose log output
```

### SEE ALSO

* [firefly](firefly.md)	 - FireFly CLI is a developer tool used to manage local development stacks

###### Auto generated by spf13/cobra on 30-Jun-2025
